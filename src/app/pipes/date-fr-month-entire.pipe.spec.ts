import { DateFrMonthEntirePipe } from './date-fr-month-entire.pipe';

describe('DateFrMonthEntirePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFrMonthEntirePipe();
    expect(pipe).toBeTruthy();
  });
});
