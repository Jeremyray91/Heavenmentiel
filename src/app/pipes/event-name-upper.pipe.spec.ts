import { EventNameUpperPipe } from './event-name-upper.pipe';

describe('EventNameUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new EventNameUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
