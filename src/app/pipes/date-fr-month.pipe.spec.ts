import { DateFrMonthPipe } from './date-fr-month.pipe';

describe('DateFrMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFrMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
